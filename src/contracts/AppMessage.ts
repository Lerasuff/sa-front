export enum AppMessage {
  Connect = 'connect',
  Wait = 'wait',
  Ready = 'ready',
  TimeSync = 'time_sync',
  Steps = 'steps',
  Complete = 'complete',
  BoardUpdate = 'update_cell',
  BoardReady = 'board_ready',
  EnemyDisconnect = 'enemy_disconnect',
}
